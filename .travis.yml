language: node_js
node_js:
 - "node"

cache:
  directories:
    - "node_modules"

env:
  - ENVIRONMENT=testing

before_script:
  - npm install -g istanbul coveralls mocha
  - chmod -R 777 tests/

script:
  - npm test

install:
  - npm install

after_success:
  - npm run coveralls